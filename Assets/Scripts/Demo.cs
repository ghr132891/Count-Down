using UnityEngine;

public class Demo : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("First, Hello World !");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
